import { UpvortDirective } from './upvort.directive';

describe('UpvortDirective', () => {
  it('should create an instance', () => {
    const directive = new UpvortDirective();
    expect(directive).toBeTruthy();
  });
});
